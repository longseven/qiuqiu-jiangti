# 球球讲题视频 · 开发方案

> 基于现有代码包(animate / animate_vertical / animate_fun / animate_fun_full2 / mascot / dub_full2)的现状诊断与下一阶段规划。
> 版本:v1.0 · 2026-06-11

---

## 一、目标定位

现有成品(导数压轴三问,149.5s 竖屏闯关版)已经验证了三件事:闯关叙事结构(hook → 关卡卡 → 讲解 → 通关 → 总结)是成立的;球球作为 IP 角色的表情系统(14 种)够用且统一;matplotlib 逐帧渲染 + edge-tts 配音 + ffmpeg 合成的全 Python 工具链是自包含、可复现的。

但当前模式是"一题一工程":换一道题,意味着重写场景函数(~500 行)、重配 TIMELINE、手工对齐 SEGS、重调公式排版。这与平台"批量生产讲题内容"的目标不匹配。

**本方案的核心目标:把这套代码从"一个视频的源码"重构为"一条视频生产线",使得新题的边际成本从"写代码"降到"写一份分镜 JSON(可由 LLM 起草 + 人审)"。**

与现有系统的衔接:口播稿生成器(四段式:我按题面理解为 / 匹配结果 / 讲解稿 / 通法总结)的"讲解稿"正是视频 narration 的上游;LLM 生成 + 评分校验的方法论(数学正确性硬门槛 + rubric 评分)可以直接复用到"讲稿 → 分镜"的转换环节。

---

## 二、现状诊断

### 2.1 资产盘点(可保留复用的部分)

| 资产 | 现状 | 处置 |
|---|---|---|
| 分层架构 | animate(基础)→ vertical(图象)→ fun(UI 组件)→ full2(编排) | **保留**,正是数据驱动引擎需要的分层 |
| mascot.py | 单一事实源,14 表情,参数化比例 | **保留并扩展**动作动词(滚入、指向坐标) |
| 闯关场景模板 | hook / card / part / clear / summary 五类场景 | **抽象为模板库**,所有题共用 |
| UI 特效组件 | starburst / confetti / hp_bar / shake / pop / slide_in | **保留**,改为分镜可引用的"特效动词" |
| 图象函数 | graph_part1/2/3 与本题强耦合 | **重构为题型插件**(见 4.2) |
| dub_full2.py | TTS 合成 + adelay 拼接逻辑成熟 | **保留拼接逻辑**,反转时间轴方向(见 4.1) |

### 2.2 主要痛点(按解决优先级)

**① 时间轴双向手工对齐(最高优先级)。** README 已明确警告:改 TIMELINE 必须手工同步 dub_full2.py 的 SEGS 起点并重跑配音。当前是"画面先定时长、配音去凑",每改一处文案就要人工核对五个锚点。这是批量化的第一道墙。

**② 内容硬编码。** 题面、公式、台词、屏幕大字、球球表情切换时机,全部写死在 s_part1/2/3 的 Python 代码里。内容与表现没有分离。

**③ mathtext 暗坑。** 不支持 `\tfrac`、公式内不能放中文、✓/ˣ 等字符变方块——这些坑目前靠开发者记忆规避,没有机器校验,LLM 起草分镜时必然踩。

**④ 渲染速度。** 3588 帧 2× 高清约 10 分钟,单进程。批量生产时会成为吞吐瓶颈。

**⑤ 图象与题目强耦合。** 每道题的函数图象都是手写的 matplotlib 绘图代码,这部分无法完全模板化,但可以按题型收敛为"参数化插件"。

---

## 三、总体架构

```
题目 JSON ──► 口播稿生成器(已有) ──► 讲解稿(三档难度)
                                          │
                                          ▼
                              ┌─────────────────────┐
                              │  分镜编排器 compiler  │  LLM 起草 + lint 校验 + 人审
                              └─────────────────────┘
                                          │
                                          ▼
                                  storyboard.json          ← 唯一的内容事实源
                                    │           │
              ┌─────────────────────┘           └──────────────────┐
              ▼                                                    ▼
   ┌──────────────────┐                                  ┌──────────────────┐
   │  配音器 dub        │  逐段 TTS → 测实际时长            │  渲染引擎 engine   │
   │  (audio-first)    │ ──► timeline.json(场景起止)──►   │  场景模板+题型插件  │
   └──────────────────┘                                  └──────────────────┘
              │                                                    │
              ▼                                                    ▼
        narration.m4a                                        frames/*.png
              └────────────────────┬───────────────────────────────┘
                                   ▼
                          ffmpeg 合成 out.mp4
```

五个模块,职责单一:

1. **storyboard.json(分镜脚本)**——内容与表现分离的核心。一道题一个文件,描述场景序列、每场景的台词、屏幕元素、球球表情、特效触发、图象插件引用。
2. **配音器 dub**——逐段合成 TTS,**用实测音频时长反推时间轴**,输出 timeline.json。
3. **渲染引擎 engine**——读 storyboard + timeline,用场景模板库逐帧渲染。现有 animate/fun/mascot 重构而来。
4. **题型图象插件 graphs**——每个题型(解三角形、导数、圆锥曲线…)一个插件模块,暴露统一接口。
5. **分镜编排器 compiler**——讲解稿 → storyboard 的转换,LLM 起草 + 静态校验 + 人审。

---

## 四、关键设计决策

### 4.1 音频先行(audio-first timeline)——反转当前的对齐方向

现在:先拍脑袋定 part1=32s / part2=38s / part3=44s,再写台词去凑,凑不进就裁。
改为:**先按分镜逐段合成 TTS,ffprobe 测出每段实际时长,然后 `场景时长 = max(台词时长 + 呼吸间隙, 场景最短时长)`,自动生成 TIMELINE。**

好处是文案改一个字,重跑一条命令即全片自动重对齐,彻底消灭 README 里那条"必须手工同步五个锚点"的警告。配套两条约束防节奏失控:

- 每类场景设最短/最长时长(如 card 固定 2.5s,part 上限 60s);台词超长时自动提高 TTS rate 一档(+18% → +24%)或在 lint 阶段报警要求拆段。
- 场景内的子动画锚点(公式弹出、星爆时机)改用**相对时间**(占场景时长的百分比)或**台词锚点**(对齐到某段文字的 TTS 起点,edge-tts 可输出逐句 boundary 时间戳),不再写绝对秒数。

### 4.2 storyboard.json schema(草案)

```json
{
  "meta": { "title": "导数压轴三连闯关", "ratio": "9:16", "voice": "zh-CN-XiaoyiNeural" },
  "scenes": [
    {
      "id": "part3", "template": "part", "accent": "GREEN",
      "narration": "第三关,最难的不等式!a 等于零……",
      "min_dur": 30, "max_dur": 60,
      "items": [
        { "t": 0.00, "type": "formula", "tex": "g(x)=3\\sqrt{2}\\,e^{x}\\cos(3x+\\pi/4)",
          "pos": [4.5, 11.2], "size": 30, "fx": "boxed_pop" },
        { "t": 0.25, "type": "graph", "plugin": "derivative.valley",
          "params": { "fn": "g3", "zero_band": [0.2618, 1.309] } },
        { "t": 0.25, "type": "mascot", "expr": "angry", "pos": [7.2, 4.0], "s": 0.95 },
        { "t": 0.80, "type": "fx", "name": "starburst", "pos": [4.5, 8.0], "color": "GREEN" },
        { "t": 0.90, "type": "mascot", "expr": "cheer" }
      ]
    }
  ]
}
```

要点:`t` 是场景内相对进度(0~1)或台词锚点 `"t": "@sent:3"`(第 3 句开始时);`template` 决定底层布局(沿用现有五类);`plugin` 引用题型图象插件;颜色用语义名(BLUE/GOLD/GREEN)对接 animate.py 的常量。**这个 schema 与七字段文档里的 scenePlan / boardPlan / visualPlan 同构,可以设计成由七字段文档直接降维生成。**

### 4.3 题型图象插件接口

```python
# graphs/derivative.py
def valley(gax, p, params):
    """p∈[0,1] 场景进度; params 来自 storyboard。负责画 g(x) 山谷图 + 三点标注。"""
```

每个插件只依赖 `(gax, p, params)`,不感知时间轴和场景编排。首批插件按你的题型推进顺序:**解三角形先行**(三角形动态作图、正余弦定理标注、外接圆),其次导数(切线、单调区间、山谷图——现有 graph_part1/2/3 重构即得),再圆锥曲线。每题型预计 3–6 个插件可覆盖大部分讲解需求。

### 4.4 mathtext:保留,但加 lint 前置

不引入 LaTeX/usetex(破坏自包含、CI 难装),保留 mathtext,代价是把"人记坑"变"机器查坑"。compiler 阶段对所有 `tex` 字段做静态校验:

- 禁用清单:`\tfrac`、`\text{中文}`、✓ ˣ ✗ 等已知变方块字符(给出替代写法:`\frac`、中文移出公式、✓ 用画线组件);
- 试渲染校验:每条公式离屏渲染一次,捕获 mathtext 异常,渲染失败的分镜直接打回,不进入帧渲染阶段。

这一条对 LLM 起草分镜尤其重要——它是数学正确性硬门槛之外的"渲染正确性硬门槛"。

### 4.5 渲染提速:按场景并行

场景之间无状态依赖(render_frame 本来就按 gt 独立取景),天然可并行:multiprocessing 按场景切分帧区间,每 worker 独立 figure。8 核下 10 分钟 → 预计 2 分钟以内。另加 `--proof` 模式:dpi 100 + 每秒 6 帧抽样渲染,30 秒出低清样片供人审节奏,确认后再出 2× 成品。

### 4.6 球球:从"表情"到"动作"

mascot.py 维持单一事实源不变。在引擎层补一个动作系统(不改 mascot 本体):`roll_in`(滚动入场,复用 fun 里的 ball_x 思路)、`point_at(target)`(point 表情 + 自动朝向目标坐标)、`bounce`(通关弹跳)。动作成为 storyboard 里 mascot item 的可选字段,例如 `{ "type": "mascot", "expr": "point", "action": {"point_at": [4.5, 11.2]} }`。

---

## 五、与口播稿/LLM 系统的衔接

讲解稿 → 分镜的转换是唯一需要"创作判断"的环节,方案沿用你已确认的生成-评分方法论:

1. **输入**:口播稿生成器的"讲解稿"段落(或七字段文档的 narrationDraft + boardPlan + visualPlan)。
2. **LLM 起草** storyboard:prompt 中固定五类场景模板、可用特效动词表、可用图象插件表、mathtext 写作规范——LLM 只做"填模板 + 选插件 + 定锚点",不发明新表现形式。
3. **三道门**:① 数学正确性(公式与讲稿一致性,硬门槛);② 渲染正确性(4.4 的 lint + 试渲染,硬门槛);③ rubric 评分(节奏:单场景台词密度、特效频率;表现:表情切换是否匹配情绪节点)。
4. **人审**:proof 低清样片 + storyboard 并排查看,审的是节奏和板书取舍,不审代码。

注意一个边界:**图象插件本身不交给 LLM 写**。新题型的插件由人开发一次,LLM 只能引用插件并填参数——这是保证视觉质量稳定的关键收敛点。

---

## 六、分期里程碑

### P0 · 架构验证(约 1 周)——"同一个视频,数据驱动地重做一遍"

把现有导数题的全部内容(台词、公式、大字、表情时机)从 Python 抽出为第一份 storyboard.json;实现 audio-first 时间轴;场景模板库从 full2 重构而来。**验收标准:新管线产出的视频与现有 out_full2.mp4 观感一致,且修改任意一句台词后,一条命令完成全片重对齐重渲染。** 这一步不追求新功能,只验证架构,风险最低、信息量最大。

### P1 · 题型模板化(1–2 周)——"第二道题不写渲染代码"

以解三角形为第二个题型:开发解三角形图象插件组;人工编写一份解三角形题的 storyboard(暂不接 LLM),走通全流程。同期完成并行渲染与 proof 模式。**验收标准:解三角形新题从 storyboard 到成片,零渲染代码改动。**

### P2 · LLM 编排接入(2 周)——"讲稿进,样片出"

实现 compiler:LLM 起草 + 三道门校验;打通口播稿生成器输出 → storyboard 的自动转换;沉淀分镜 prompt 与 rubric。**验收标准:给定一份已审核的讲解稿,10 分钟内自动产出可供人审的 proof 样片,渲染失败率为 0(被 lint 拦截而非渲染期报错)。**

### P3 · 规模化(持续)

批量任务队列(一晚 N 题);封面图自动生成(复用 mascot sprites + 题目大字);A/B 钩子变体(口播文案.md 里的备用钩子机制产品化:同一 storyboard 换 hook 场景出多版本);视频元数据(标题/话题标签)随 storyboard 一并生成。

---

## 七、风险与对策

| 风险 | 对策 |
|---|---|
| LLM 分镜的数学/渲染错误 | 双硬门槛前置(公式一致性 + lint 试渲染),错误在进帧渲染前拦截 |
| TTS 时长波动导致节奏失控 | 场景最短/最长时长约束 + 超长自动调 rate / 报警拆段 |
| 题型插件开发成为新瓶颈 | 插件粒度收敛(每题型 3–6 个),按平台题型推进顺序排期,解三角形先行 |
| 字体环境差异(YaHei 仅 Windows) | 引擎启动时字体探测降级链(YaHei → SimHei → Noto Sans CJK),CI 内置 Noto |
| storyboard schema 过早固化 | P0/P1 期间 schema 标 draft,P2 接 LLM 前冻结 v1 并写 JSON Schema 校验 |

---

## 八、立即可做的三件事

1. **P0 启动**:把现有 TIMELINE + SEGS + 各 s_part 的文案抽成第一份 storyboard.json(纯数据迁移,半天)。
2. **dub 重写**:实现 audio-first(逐段合成 → ffprobe 测长 → 生成 timeline.json),这是收益/成本比最高的单点改造。
3. **mathtext lint 清单**:把 README"改动须知"里的坑 + 这次踩过的所有公式问题整理成机器可查的规则表,后面 LLM 起草直接复用。
